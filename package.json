{
  "name": "reveal-start-template",
  "version": "1.0.0",
  "description": "Reveal.js starter template",
  "main": "index.js",
  "scripts": {
    "setup": "npm install && bower install && npm run build",
    "build": "gulp build",
    "watch": "gulp watch",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Artem Demo",
  "devDependencies": {
    "babel-core": "^6.4.0",
    "babel-preset-es2015": "^6.3.13",
    "gulp": "^3.9.0",
    "gulp-compile-handlebars": "^0.6.1",
    "gulp-concat": "^2.6.0",
    "gulp-less": "^3.0.5",
    "gulp-rename": "^1.2.2",
    "gulp-util": "^3.0.7"
  },
  "dependencies": {
    "font-awesome": "^4.5.0"
  }
}
